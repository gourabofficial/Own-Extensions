{
  "manifest_version": 3,
  "name": "Background Clock Extension",
  "version": "1.0",
  "description": "Displays a clock as the background on every new tab.",
  "chrome_url_overrides": {
    "newtab": "popup.html"
  },
  "permissions": ["storage"],
  "action": {
    "default_popup": "popup.html"
  }
}
